name = "amp-hubspot-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

[vars]
ALLOWED_ORIGIN = "https://ampagency.github.io"

# Optional — only needed if you want the token server-side:
# npx wrangler secret put HUBSPOT_TOKEN
